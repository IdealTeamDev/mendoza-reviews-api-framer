# Reglas del proyecto

- **Mensajes de Git Commit**:
  - Los comentarios/mensajes de commit deben ser **siempre en español**.
  - No hacer `git commit` ni `git push` automáticamente. El usuario debe indicar cuándo actualizar Git y **aprobar explícitamente** el mensaje del commit antes de realizar la acción.
